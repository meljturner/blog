      <li>
        <article class="snippet">
          <img class="snippet-image" src="{{ post.data.image | imageUrl}}" alt="{{post.data.caption}}">
          <h3 class="snippet-title"><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          <p class="snippet-meta">by <span>{{ post.data.author }} on {{ post.data.date | postDate }}</span></p>
          <p class="snippet-body">{{ post.data.description }}</p>
          <a href="{{ post.url }}" class="btn btn--primary">Continue reading</a>
        </article>
      </li>
